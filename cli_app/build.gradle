apply plugin: 'java'
apply plugin: 'application'

dependencies {
    compile project(':core')
}

run {
    mainClassName = 'org.spargonaut.games.Application'
    args = ['interactive', 'src/main/resources/freshGame.txt', 'human', '2']
    standardInput = System.in
}